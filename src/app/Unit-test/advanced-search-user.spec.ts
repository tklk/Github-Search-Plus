import { AdvancedSearchUser } from '../Interface/advanced-search-user';

describe('AdvancedSearchUser', () => {
  it('should create an instance', () => {
    expect(new AdvancedSearchUser()).toBeTruthy();
  });
});
