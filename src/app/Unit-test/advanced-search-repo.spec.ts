import { AdvancedSearchRepo } from '../Interface/advanced-search-repo';

describe('AdvancedSearchRepo', () => {
  it('should create an instance', () => {
    expect(new AdvancedSearchRepo()).toBeTruthy();
  });
});
